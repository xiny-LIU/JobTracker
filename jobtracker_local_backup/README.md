# JobTracker

> 专为应届生设计的求职管理工具。纯前端实现，默认使用浏览器本地保存，并支持 JSON 文件导出/导入备份。

## 特点

- ✅ **零后端、零数据库、零成本** —— 直接用浏览器即可运行
- ✅ **本地保存** —— 数据保存在当前浏览器的 localStorage 中
- ✅ **JSON 备份/迁移** —— 一键导出备份文件，换设备时导入即可恢复
- ✅ **面试准备包** —— 一键生成可打印的面试速查单页
- ✅ **数据洞察** —— 自动统计面试通过率、简历版本效果、知识盲区词云
- ✅ **更稳定** —— 不再依赖 GitHub Token、Gist ID 或外部网络接口

## 本地使用

只需一个现代浏览器，无需安装任何工具：

1. 下载本项目所有文件
2. 用浏览器打开 `index.html`
3. 开始使用

## 数据备份与迁移

### 导出备份

1. 打开网页，进入「设置」页
2. 点击「导出 JSON 备份」
3. 保存下载得到的 `jobtracker_日期.json` 文件

### 导入备份

1. 在另一台设备或另一个浏览器打开本项目
2. 进入「设置」页
3. 点击「导入 JSON」
4. 选择之前导出的备份文件

注意：导入会覆盖当前浏览器中的旧数据。导入前请确认是否需要先导出一份旧数据备份。

## 项目结构

```text
jobtracker/
├── index.html          # 页面入口
├── css/
│   └── style.css       # 样式
├── js/
│   ├── data.js         # 本地数据管理（localStorage）
│   └── app.js          # 界面与交互逻辑
└── README.md
```

## 技术栈

- 原生 HTML5 / CSS3 / JavaScript（无框架）
- localStorage 本地持久化
- JSON 文件导出/导入
- GitHub Pages 免费托管

## 数据安全

- 所有数据首先保存在当前浏览器本地
- 导出的 JSON 文件只会保存到你自己的电脑
- 项目不再保存 GitHub Token
- 项目不再向 GitHub Gist API 上传数据

## License

MIT
